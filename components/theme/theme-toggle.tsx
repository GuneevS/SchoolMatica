"use client";

// Light mode only - theme toggle disabled
// This component returns null as dark mode has been removed from the application
export function ThemeToggle() {
  // Dark mode is disabled - returning null to hide the toggle
  return null;
}

